<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Color for the ON state (checked) -->
    <item
        android:color="@color/lightMode_primary_50_opacity_color"
        android:state_checked="true" />

    <!-- Color for the OFF state (unchecked) -->
    <item
        android:color="@color/darkMode_tertiary_color"
        android:state_checked="false" />

</selector>